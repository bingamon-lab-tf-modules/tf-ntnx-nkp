locals {
  # TODO: Add computed locals when NKP resources are available.
  #
  # Example computed locals:
  # - cluster_endpoints: Map of cluster names to their API endpoints
  # - total_worker_nodes: Sum of all node pool instances across clusters
  # - cluster_node_pools: Mapping of clusters to their associated node pools
  # - registry_urls: List of all configured registry URLs
}
